# ─── Rigel Production Environment ───
# install.sh가 자동 생성하므로 수동 설정은 보통 불필요합니다.
# supabase status 출력에서 값을 가져옵니다.

PUBLIC_SUPABASE_URL=http://localhost:54321
PUBLIC_SUPABASE_ANON_KEY=여기에_supabase_status_의_anon_key

SITE_URL=http://localhost:3000
APP_PORT=3000
