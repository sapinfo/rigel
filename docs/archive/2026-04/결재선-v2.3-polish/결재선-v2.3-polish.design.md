# Design — 결재선 v2.3 Polish

> 참조: Plan `결재선-v2.3-polish.plan.md`
> Migration: 0053

---

## M1: StampLine 컴포넌트 분리

- `$lib/components/StampLine.svelte` 신규 생성 (기안 인라인 코드 추출)
- Props: `steps: StampStep[]`, `drafterName: string`
- 문서 상세 `documents/[id]/+page.svelte`에 DocumentSteps 위에 StampLine 추가
- 기안 `drafts/new/[formCode]/+page.svelte`에서 인라인 → StampLine 컴포넌트 교체

## M2: OrgTreePicker 통합

- `drafts/new/[formCode]/+page.server.ts` load에 departments 추가
- `drafts/new/[formCode]/+page.svelte`에 ApprovalLine 위 "조직도에서 선택" 토글 추가
- OrgTreePicker onSelect → approvalLine에 push

## M3: 문서 이관 (transfer)

- `documents/[id]/+page.server.ts`에 `transfer` action 추가
- 관리자만 가능 (is_tenant_admin 체크)
- UPDATE approval_documents SET drafter_id, audit_log INSERT

## M4: 보관기한 + 보안등급

- Migration 0053: `approval_documents`에 `retention_period text DEFAULT '5년'`, `security_level text DEFAULT '일반'` 추가
- 기안 페이지 "문서 설정" 섹션에 드롭다운 추가
- submit action에서 UPDATE (urgency와 동일 패턴)

## M5: admin 사이드바

- `+layout.svelte` 내 admin 네비에 신규 라우트 링크 추가
